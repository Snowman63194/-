#include<stdio.h>
int main()
{
	int n,a,b,c,d;
	scanf("%d", &n);
	if (n % 2 != 0)
		printf("0 0");
	else if (n % 2 == 0)
	{
		d = n / 2;
		b = (n % 4) / 2;
		a = n / 4;
		c = a + b;
		printf("%d %d", c, d);
	}

	return 0;
}

